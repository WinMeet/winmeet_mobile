import 'package:flutter/material.dart';

class ScheduledEventsView extends StatelessWidget {
  const ScheduledEventsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Scheduled Events View"),
      ),
    );
  }
}
